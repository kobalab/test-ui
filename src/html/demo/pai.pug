doctype html
html(lang="ja")
  head
    include ../inc/meta
    title pai
    include ../inc/style
    include ../inc/script
    script.
        $(function(){
            const pai = Majiang.UI.pai('#loaddata');
            for (let s of ['m','p','s','z']) {
                for (let n = 0; n <= 9; n++) {
                    $('div').append(pai(Majiang.Shoupai.valid_pai(s+n)||"_"));
                }
                $('div').append($('<br>'));
            }
        });
  body.pai
    h1 pai
    include ../inc/loaddata
    div
